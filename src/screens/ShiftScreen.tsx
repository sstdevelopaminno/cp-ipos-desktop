import { useState } from "react"; import type { PosRepository } from "../data/repository"; import type { Shift, Staff } from "../domain/types";
export function ShiftScreen({repo,staff,onReady}:{repo:PosRepository;staff:Staff;onReady:(s:Shift)=>void}){ const [cash,setCash]=useState("0"); const open=async()=>onReady(await repo.openShift(Number(cash)||0)); return <main className="center-screen"><section className="shift-card"><span className="status-pill">OFFLINE READY</span><h1>เปิดกะการขาย</h1><p>{staff.displayName}</p><label>เงินสดเริ่มต้น<input value={cash} onChange={e=>setCash(e.target.value)} inputMode="decimal" /></label><button className="big-primary" onClick={open}>เปิดกะและเริ่มขาย</button></section></main> }
